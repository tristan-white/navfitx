# TODO: Add tests for the GUI using pytest-qt package.

"""
import pytest

from typer.testing import CliRunner

from navfitx.cli import app

runner = CliRunner()


def test_app():
    result = runner.invoke(app, ["gui"])
    assert result.exit_code == 0
"""
